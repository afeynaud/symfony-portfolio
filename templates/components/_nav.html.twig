<nav class="navbar navbar-expand-lg navbar-dark mt-3">
    <div class="container-fluid">
        <a class="navbar-brand ms-3 text-black" href="{{ path('home_index') }}">Alexandre Feynaud</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown"
                aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse justify-content-end" id="navbarNavDropdown">
            <ul class="navbar-nav">
                <li class="nav-item pe-3">
                    <a class="nav-link" href="#"></a>
                </li>
                <li class="nav-item pe-3">
                    <a class="nav-link" href="#"></a>
                </li>
                {% if is_granted('ROLE_ADMIN') %}
                    <li class="nav-item dropdown profile-picture d-flex flex-row pe-5 me-5">
                        <a class="nav-link dropdown-toggle text-black" href="#" id="navbarDropdownMenuLink"
                           data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                            Admin
                        </a>
                        <div class="dropdown-menu background-primary border text-center" aria-labelledby="navbarDropdownMenuLink">
                            <a class="dropdown-item nav-link text-black" href="">Administration</a>
                            <a class="dropdown-item nav-link text-black" href="{{ path('app_logout') }}">Se déconnecter</a>
                        </div>
                    </li>
                {% else %}
                {% endif %}
            </ul>
        </div>
    </div>
</nav>
