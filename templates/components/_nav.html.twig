<nav class="navbar navbar-expand-lg nav-settings">
    <div class="container-fluid">
        <a class="navbar-brand ms-3 text-black" href="{{ path('home_index') }}">Alexandre Feynaud</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        {% if is_granted('ROLE_ADMIN') %}
        <div class="collapse navbar-collapse d-flex justify-content-end" id="navbarNav">
            <ul class="navbar-nav">
                <li>
                    <a class="dropdown-item nav-link text-black" href="{{ path('admin_index') }}">
                        Administration
                    </a>
                </li>
                <li>
                    <a class="dropdown-item nav-link text-black" href="{{ path('app_logout') }}">
                        Se déconnecter
                    </a>
                </li>
            </ul>
        </div>
        {% endif %}
    </div>
</nav>
